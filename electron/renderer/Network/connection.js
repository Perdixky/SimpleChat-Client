// WebSocket connection and routing (planned extraction)
// Responsibilities:
// - manage TLS/WebSocket connection
// - heartbeat
// - route requests/responses
export function createConnection(/* opts */) {
  return {
    connect() {},
    disconnect() {},
  };
}

